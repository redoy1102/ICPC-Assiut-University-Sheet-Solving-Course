#include<bits/stdc++.h>
using namespace std;

int main(){
    char a[200], b[200], c[200], d[200];

    cin >> a >> b;
    cin >> d >> d;
    
    if(strcmp(b, d) == 0)
    {
        printf("ARE Brothers\n");
    }
    else{
        printf("NOT\n");
    }

    return 0;
}