#include<stdio.h>
int main(){
    int a, b, c = 0;
    scanf("%d%d", &a, &b);
    

    for(int i = a; i <= b; i++)
    {
        while(i != 0)
        {
            int ld = i %10;
            if(ld == 4 or ld)
        }
    }
    if(c == 0)
    {
        printf("-1\n");
    }


    return 0;
}